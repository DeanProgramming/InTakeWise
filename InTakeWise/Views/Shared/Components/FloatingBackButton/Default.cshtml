@model InTakeWise.ViewComponents.FloatingBackButtonVm

<a class="floating-back-btn"
   asp-controller="@Model.Controller"
   asp-action="@Model.Action"
   aria-label="Back to Home">
    <span>@Model.Text</span>
</a>
